<?php 
/**
 * Implements hook_preprocess_node().
 */
function body_trimmer_preprocess_node(array &$variables) {
    $node = $variables['node'];
    if(($node->getType() == 'product')) {
      $variables['#attached']['library'][] = 'body_trimmer/trimmer-js';
    }
}
